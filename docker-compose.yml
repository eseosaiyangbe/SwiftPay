x-default-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"

# #### Docker Compose Configuration ####
# #### This file defines all services needed to run SwiftPay locally ####

services:
  # ============================================
  # INFRASTRUCTURE SERVICES
  # ============================================
  
  # #### PostgreSQL Database ####
  # #### This stores all persistent data (users, wallets, transactions) ####
  postgres:
    image: postgres:15-alpine  # Use PostgreSQL version 15 with Alpine Linux (smaller image)
    logging: *default-logging
    environment:
      POSTGRES_DB: swiftpay      # Database name
      POSTGRES_USER: swiftpay    # Database user
      POSTGRES_PASSWORD: swiftpay123  # Database password (use secrets in production!)
    ports:
      - "5432:5432"  # Expose PostgreSQL port to host
    volumes:
      - postgres_data:/var/lib/postgresql/data  # Persist database data
      - ./migrations:/docker-entrypoint-initdb.d  # Run database migrations on startup
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U swiftpay"]  # Check if database is ready
      interval: 10s    # Check every 10 seconds
      timeout: 5s      # Wait 5 seconds for response
      retries: 5       # Try 5 times before marking as unhealthy
    restart: always
    networks:
      - swiftpay-network  # Connect to our custom network

  # #### Redis Cache ####
  # #### This stores temporary data (sessions, cache, rate limiting) ####
  redis:
    image: redis:7-alpine  # Use Redis version 7 with Alpine Linux
    logging: *default-logging
    ports:
      - "6379:6379"  # Expose Redis port to host
    volumes:
      - redis_data:/data  # Persist Redis data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]  # Check if Redis is responding
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - swiftpay-network

  # #### RabbitMQ Message Queue ####
  # #### This handles asynchronous communication between services ####
  # #### Used for transaction processing and notifications ####
  rabbitmq:
    image: rabbitmq:3-management-alpine # #### RabbitMQ with management UI ####
    logging: *default-logging
    ports:
      - "5672:5672"   # #### AMQP port for message publishing ####
      - "15672:15672" # #### Management UI port ####
    environment:
      RABBITMQ_DEFAULT_USER: swiftpay     # #### RabbitMQ username ####
      RABBITMQ_DEFAULT_PASS: swiftpay123  # #### RabbitMQ password ####
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq  # #### Persist RabbitMQ data ####
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"] # #### Check if RabbitMQ is responding ####
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - swiftpay-network

  # ============================================
  # MONITORING STACK
  # Start with: docker compose --profile monitoring up
  # Access: Prometheus http://localhost:9090  Grafana http://localhost:3006 (admin/admin)
  #         Alertmanager http://localhost:9093
  # ============================================
  prometheus:
    profiles: ["monitoring"]
    image: prom/prometheus:latest
    logging: *default-logging
    ports:
      - "9090:9090"
    volumes:
      # NOTE: prometheus.yml uses Kubernetes FQDNs (*.svc.cluster.local) — targets will show
      # as DOWN in Docker Compose, but Prometheus UI, alerts, and Grafana dashboards are accessible.
      - ./k8s/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./k8s/monitoring/alerts.yml:/etc/prometheus/alerts.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
    networks:
      - swiftpay-network
    restart: unless-stopped

  grafana:
    profiles: ["monitoring"]
    image: grafana/grafana:latest
    logging: *default-logging
    ports:
      - "3006:3000"
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./k8s/monitoring/grafana-dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./k8s/monitoring/grafana-datasources.yml:/etc/grafana/provisioning/datasources/datasource.yml:ro
    depends_on:
      - prometheus
    networks:
      - swiftpay-network
    restart: unless-stopped

  alertmanager:
    profiles: ["monitoring"]
    image: prom/alertmanager:latest
    logging: *default-logging
    ports:
      - "9093:9093"
    volumes:
      - ./k8s/monitoring/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager_data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
    networks:
      - swiftpay-network
    restart: unless-stopped

  # ============================================
  # LOGGING STACK
  # Start with: docker compose --profile logging up
  # (or combine: docker compose --profile monitoring --profile logging up)
  # Access: Loki http://localhost:3100  (add as datasource in Grafana)
  # ============================================
  loki:
    profiles: ["logging"]
    image: grafana/loki:latest
    logging: *default-logging
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - loki_data:/loki
    networks:
      - swiftpay-network
    restart: unless-stopped

  promtail:
    profiles: ["logging"]
    image: grafana/promtail:latest
    logging: *default-logging
    volumes:
      - /var/log:/var/log:ro
      - ./services:/services:ro
      - ./k8s/monitoring/promtail-config.yml:/etc/promtail/config.yml:ro
    command: -config.file=/etc/promtail/config.yml
    networks:
      - swiftpay-network
    restart: unless-stopped

  # ============================================
  # APPLICATION SERVICES
  # ============================================
  auth-service:
    build:
      context: ./services
      dockerfile: auth-service/Dockerfile
    logging: *default-logging
    ports:
      - "3004:3004"
    environment:
      PORT: 3004
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: swiftpay
      DB_USER: swiftpay
      DB_PASSWORD: swiftpay123
      REDIS_URL: redis://redis:6379
      JWT_SECRET: J8vmoWX/NIIcBunMHhkMmok7ZViKS/OaQovGmp9ETm2+puxFEL/lt+egqTNRLM3J
      JWT_EXPIRES_IN: 24h
      NODE_ENV: development
      SERVICE_NAME: auth-service
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      swiftpay-network:
      observability-net:
        aliases:
          - swiftpay-auth-service
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3004/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  wallet-service:
    build:
      context: ./services
      dockerfile: wallet-service/Dockerfile
    logging: *default-logging
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: swiftpay
      DB_USER: swiftpay
      DB_PASSWORD: swiftpay123
      REDIS_URL: redis://redis:6379
      NODE_ENV: development
      CONTAINER_ENV: "true"
      SERVICE_NAME: wallet-service
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
    networks:
      swiftpay-network:
      observability-net:
        aliases:
          - swiftpay-wallet-service

  transaction-service:
    build:
      context: ./services
      dockerfile: transaction-service/Dockerfile
    logging: *default-logging
    ports:
      - "3008:3002"
    environment:
      PORT: 3002
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: swiftpay
      DB_USER: swiftpay
      DB_PASSWORD: swiftpay123
      RABBITMQ_URL: amqp://swiftpay:swiftpay123@rabbitmq:5672
      REDIS_URL: redis://redis:6379
      WALLET_SERVICE_URL: http://wallet-service:3001
      NODE_ENV: development
      CONTAINER_ENV: "true"
      SERVICE_NAME: transaction-service
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      wallet-service:
        condition: service_healthy
    networks:
      swiftpay-network:
      observability-net:
        aliases:
          - swiftpay-transaction-service
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  notification-service:
    build:
      context: ./services
      dockerfile: notification-service/Dockerfile
    logging: *default-logging
    ports:
      - "3003:3003"
    environment:
      PORT: 3003
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: swiftpay
      DB_USER: swiftpay
      DB_PASSWORD: swiftpay123
      RABBITMQ_URL: amqp://swiftpay:swiftpay123@rabbitmq:5672
      NODE_ENV: development
      CONTAINER_ENV: "true"
      SERVICE_NAME: notification-service
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      swiftpay-network:
      observability-net:
        aliases:
          - swiftpay-notification-service
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3003/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  api-gateway:
    build:
      context: ./services
      dockerfile: api-gateway/Dockerfile
    logging: *default-logging
    ports:
      - "3007:3000"
    environment:
      PORT: 3000
      # Service URLs — Docker Compose DNS uses service names as hostnames
      AUTH_SERVICE_URL: http://auth-service:3004
      WALLET_SERVICE_URL: http://wallet-service:3001
      TRANSACTION_SERVICE_URL: http://transaction-service:3002
      NOTIFICATION_SERVICE_URL: http://notification-service:3003
      # Database — required for the health-check connection pool (server.js:74)
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: swiftpay
      DB_USER: swiftpay
      DB_PASSWORD: swiftpay123
      REDIS_URL: redis://redis:6379
      JWT_SECRET: J8vmoWX/NIIcBunMHhkMmok7ZViKS/OaQovGmp9ETm2+puxFEL/lt+egqTNRLM3J
      NODE_ENV: development
      SERVICE_NAME: api-gateway
      # Empty string is allowed only in development and accepts local browser origins.
      # Production must list explicit trusted origins, for example: https://swiftpay.example.com
      CORS_ORIGIN: ""
    depends_on:
      - auth-service
      - wallet-service
      - transaction-service
      - notification-service
    networks:
      swiftpay-network:
      observability-net:
        aliases:
          - swiftpay-api-gateway
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # ============================================
  # FRONTEND
  # ============================================
  frontend:
    # REACT_APP_* values are BUILD-TIME variables baked into the JS bundle.
    # Do NOT pass REACT_APP_API_URL here — the Dockerfile default (/api) produces the correct relative
    # URL that works in Docker Compose, MicroK8s, EKS, and AKS without rebuilding.
    # REACT_APP_SESSION_TIMEOUT_MS defaults to 900000 (15 minutes) in the Dockerfile.
    # The same image built here can be pushed to any registry and deployed to K8s.
    build:
      context: ./services
      dockerfile: frontend/Dockerfile
    logging: *default-logging
    ports:
      - "8081:80"
    environment:
      # nginx proxies /api/* → api-gateway at this URL (substituted at container start)
      API_GATEWAY_URL: http://api-gateway:3000
      # Docker's internal DNS resolver (127.0.0.11). K8s deployments use kube-dns instead.
      NGINX_RESOLVER: "127.0.0.11"
    depends_on:
      - api-gateway
    restart: unless-stopped
    networks:
      swiftpay-network:
      observability-net:
        aliases:
          - swiftpay-frontend

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
  prometheus_data:
  grafana_data:
  alertmanager_data:
  loki_data:

networks:
  swiftpay-network:
    driver: bridge
  observability-net:
    name: observability-net
    external: true
