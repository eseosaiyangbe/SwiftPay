apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swiftpay
  namespace: swiftpay-prod
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - www.swiftpay.prod.example.com
        - swiftpay.prod.example.com
      secretName: swiftpay-frontend-prod-tls
    - hosts:
        - api.swiftpay.prod.example.com
      secretName: swiftpay-api-prod-tls
  rules:
    - host: www.swiftpay.prod.example.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    - host: swiftpay.prod.example.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    - host: api.swiftpay.prod.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 80
